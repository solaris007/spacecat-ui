import React from 'react';
import SitesList from './SitesList';

const Sites = () => {
  return (
    <div>
      <h1>Sites Collection</h1>
      <SitesList />
    </div>
  );
};

export default Sites;
